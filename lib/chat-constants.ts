/** MIME type for dragging a single thread row onto the pin strip (HTML5 DnD). */
export const THREAD_DRAG_MIME = "application/x-ali-chat-thread"

/** Maximum threads that can appear as compact pin tabs in the sidebar. */
export const MAX_PINS = 8
